# Docker Compose / local deployment (C6)
# Copy to .env in project root: cp env.example .env (or on Windows: copy env.example .env)
# Never commit .env.

# Dev-friendly default for local Docker. For production, use a strong, unique password.
MONGO_INITDB_ROOT_USERNAME=admin
MONGO_INITDB_ROOT_PASSWORD=Admin1234567890
